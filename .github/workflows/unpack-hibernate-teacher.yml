name: Unpack Hibernate teacher project

on:
  push:
    paths:
      - 'src/hibernate-teacher-upload.zip'
      - '.github/workflows/unpack-hibernate-teacher.yml'

permissions:
  contents: write

jobs:
  unpack:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unpack project
        shell: bash
        run: |
          archive="src/hibernate-teacher-upload.zip"
          destination="src/HibernateTeacherValidation"

          if [ ! -f "$archive" ]; then
            echo "Archive is already removed."
            exit 0
          fi

          if [ -e "$destination" ]; then
            echo "Destination already exists: $destination"
            exit 1
          fi

          unzip -q "$archive" -d "src"
          git rm "$archive"
          git add -f "$destination"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Add Hibernate teacher validation project"
          git push
